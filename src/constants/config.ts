/**
 * File formats not allowed extensions
 */
export const FORBIDDEN_EXTENSIONS = [".js", ".ts", ".d.ts", ".json"];

/**
 * File formats allowed extensions
 */
export const ALLOWED_EXTENSIONS = [".txt", ".log"];

/**
 * Prefix
 * 
 * Defalt discord bot prefix
 * @type {"++"}
 */
export const prefix: `++` = "++";